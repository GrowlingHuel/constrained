TETHER — PROJECT TLDR
For handoff to another Claude agent
=====================================

WHAT IT IS
----------
Tether is a desktop writing constraint engine. The user picks a rule — every
word must be exactly 4 letters, or each word must start with the next letter
of the alphabet, or no word root can be repeated — and Tether enforces it in
real time, measuring compliance as the user types. It's a creative writing
tool for writers who use constraints as a generative technique (Oulipo, etc).

Now includes a Game Mode with Challenge and Score Attack sub-modes, progression
locks, streak multipliers, per-word score feedback, and persistent leaderboards.

GITHUB
------
https://github.com/HyperArkStudios/tether
(Previously GrowlingHuel/tether — GitHub redirects, but remote is updated)
Developer: Jesse Clark, Linux Mint, ~/Projects/typing/

CURRENT VERSION
---------------
v0.4.0 — game-mode branch, in development. Adds full Game Mode.
Main branch is master.

STACK
-----
- Language: Python 3.10
- Modules: tether.py (main app), constants.py, wordlist.py, analyzer.py, scorer.py
- GUI: tkinter (stdlib only — no external GUI deps)
- Optional NLP: spaCy (en_core_web_sm) + textstat for linguistic analysis
- Wordlist: SCOWL-80, 357,325 words at ~/Projects/typing/wordlist.txt
- Word rarity: word_rarity.json (185,981 entries, 3MB) generated from SCOWL
  tier wordlists (35/60/80). Common=×1.0 (omitted), Uncommon=×1.25, Rare=×1.5
- SCOWL source: cloned to ~/Projects/scowl/ for rarity tier generation
- Persistent data: ~/.tether/history.json, presets.json, scores.json, seen_onboarding
- Build: Nuitka --onefile, via GitHub Actions
- Distribution: GitHub Releases (direct binary download, no installer)

BUILT-IN CONSTRAINTS
--------------------
wordLength      — every word must be exactly N letters (counts alpha chars only,
                  apostrophes ignored: "we're" = 4 letters)
alphaCycle      — each word starts with the next alphabet letter (A→B→C...)
noRepeat        — no repeated root words (lemma-aware via spaCy)
wordGoal        — must reach N words
timeLimit       — session ends at N seconds
startLetterMax  — each starting letter used at most N times
pangram         — use every alphabet letter within N words
dictCheck       — every word must be in the wordlist

BUILT-IN PRESETS (3)
---------------------
Free Write, 500-Word Sprint, 1000-Word Sprint
(Old presets like Three/Four/Five-Letter Words, Alphabet Cycle, etc. were
 moved exclusively into Game Mode challenges.)

GAME MODE
---------
Toggle: WRITE / GAME switch at top of sidebar. Game mode replaces sidebar
with game panel. Two sub-modes:

Challenge Mode:
- 15 curated challenges covering all constraint types
- Progression locks: some challenges require ★ on a prerequisite challenge
- Star rating (★ to ★★★) based on compliance, time, and score
- Word-goal challenges count UNIQUE compliant words only (repeats don't advance)
- "were" and "we're" count as separate unique words
- Flow: List → Info panel → Start → Countdown → Play → Results → Retry/Next/Back

  Unlock chains:
  - Three's Company → Short and Sweet
  - Five Alive → Verbose
  - Alphabet Run → Double Alphabet → Tight Alphabet
  - No Repeats → Marathon
  - Four-Letter Words → Four-Letter Sprint → The Gauntlet

Score Attack Mode:
- 5 game-specific presets: Blitz, Endurance, Alphabet Grind, Word Hoarder, Free Attack
- Timed (2m/5m/10m) or endless sessions
- No progression locks — all presets available always
- Info panel with description before starting (same flow as Challenge)

Scoring Engine (shared):
  word_score = base_points × difficulty_multiplier × streak_multiplier × rarity_bonus
  - Base points: word length in alpha characters
  - Difficulty multiplier: derived from active constraints
  - Streak multiplier: ×1.0 (0-9) → ×1.5 (10-24) → ×2.0 (25-49) → ×3.0 (50-99) → ×4.0 (100+)
  - Rarity bonus: from word_rarity.json (common ×1.0, uncommon ×1.25, rare ×1.5)
  - Violation penalties by difficulty:
    Easy = streak reset only
    Medium = streak reset + flat −5 pts
    Hard = streak reset + escalating (−5, −10, −20, −40, doubles per violation in 30s window)

Game UI:
- HyperCard / System 7 aesthetic app-wide (black/white, Courier, heavy borders)
- Per-word feedback: "+N" or "+N ×2.0" flashes; "STREAK LOST" on violation
- Streak display: color escalates (white → yellow → orange → red → magenta)
- Live sidebar: challenge name, score (36pt), streak, timer, word progress
- Results panel: stars, score, stats, NEW HIGH SCORE callout, unlock notifications
- Persistent leaderboard: ~/.tether/scores.json, per-challenge per-difficulty

USER FEATURES
-------------
- Three views: Editor, History, Builder
- Constraint Builder: mix any constraints, check feasibility, save as named preset
- Persistent history: all saved sessions survive between launches
- Persistent custom presets: user-built presets saved to disk
- Live stats: word count, WPM, compliance %, violations, timer
- Violation panel: red/amber/blue severity levels, updates on every keystroke
- Language panel: richness, repeats, unknown words, Flesch readability,
  grade level, Gunning Fog, avg word length (1.5s debounce, spaCy+textstat)
- Pangram tracker: shows missing letters in real time
- Alphabet cycle tracker: shows next required letter large in sidebar
- Letter frequency reference chart
- Export session as .txt to Desktop
- First-run onboarding tour (8-step tooltip overlay, dismisses permanently)
- Update notifications: checks GitHub API on launch, shows banner if newer
  version exists (background thread, fails silently if offline)

GITHUB ACTIONS BUILD PIPELINE
------------------------------
Trigger: any tag matching v*
Matrix: ubuntu-22.04 (tether-linux), macos-latest (tether-macos),
        windows-2022 (tether.exe)
Nuitka flags: --onefile --enable-plugin=tk-inter
              --include-data-dir=<spacy_model>=en_core_web_sm
              --include-data-file=wordlist.txt=wordlist.txt
              --include-data-file=word_rarity.json=word_rarity.json
              --include-package-data=pyphen
Binary sizes: Linux ~410MB, macOS ~73MB, Windows ~91MB (spaCy is large)
Release: softprops/action-gh-release@v2 auto-attaches binaries to GitHub Release

RELEASE CHECKLIST (important — learned the hard way)
-----------------------------------------------------
Before tagging any release, THREE things must match:
  1. VERSION = "X.Y.Z"          (in constants.py)
  2. self.root.title("TETHER  vX.Y.Z")   (in tether.py __init__)
  3. git tag -a vX.Y.Z          (the actual tag)
All three must be identical or the update notifier will falsely trigger.

KNOWN HISTORY / GOTCHAS
------------------------
- App was previously called "Constrained" — renamed to Tether at v0.2.3+
- Local project folder is ~/Projects/typing/ (not ~/Projects/tether/)
- GitHub repo moved from GrowlingHuel/tether to HyperArkStudios/tether
- build.yml has been rewritten from scratch several times due to download
  rename issues (browser saves as "build (1).yml") — always edit build.yml
  directly on GitHub web editor, never via download
- macos-13 runner is retired — always use macos-latest
- Linux and macOS both output .bin by default — must use
  --output-filename=${{ matrix.binary_path }} to avoid naming collision
- spaCy model must be explicitly included via --include-data-dir or the
  binary ships without linguistic features
- pyphen must be included via --include-package-data=pyphen or Save crashes
  in the binary (textstat uses pyphen internally)
- textstat calls wrapped in try/except for Nuitka compatibility
- Do NOT commit Nuitka binaries to git — they're ~410MB each. Use .gitignore.
  Binaries are distributed via GitHub Releases only.

DEVELOPER PREFERENCES
---------------------
- Minimal dependencies
- Simplest thing that works
- No frameworks unless they earn their place
- Iterative: get something working fast, then refine
- Comfortable with: Python, tkinter, GitHub Actions, Nuitka, git CLI

ROADMAP (informal)
------------------
v0.4.0 — Game Mode release (current, in development on game-mode branch)
Phase 2 — Analytics: session history graphs, per-challenge stats, progress over time
Future  — README expansion with download links and screenshots
Future  — consider web version (Phoenix LiveView on Fly.io) for broader reach
